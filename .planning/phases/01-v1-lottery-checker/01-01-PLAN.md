---
phase: 01-v1-lottery-checker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - REQ-03
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "Page background is plain white, not a gold/cream gradient"
    - "CSS custom properties reflect Thai lottery green/red palette, not gold/cream"
    - "All Tailwind utility classes referencing new tokens resolve correctly (no broken styles)"
  artifacts:
    - path: src/app/globals.css
      provides: "@theme block with Thai lottery color tokens replacing gold/cream tokens"
      contains: "--color-lottery-green"
  key_links:
    - from: src/app/globals.css
      to: src/components/*.tsx
      via: "Tailwind CSS token resolution"
      pattern: "--color-lottery-green"
---

<objective>
Replace the @theme color tokens in globals.css with the Thai lottery green/red palette. This is a pure token swap — no component logic changes. Every subsequent component restyle in Plans 02 and 03 depends on these tokens being defined.

Purpose: The current gold/cream palette (#D4A017, #FFFDF7) does not match the Thai lottery identity the user specified (D-05, D-07). This plan establishes the correct color system.

Output: Updated globals.css with new @theme tokens and plain white body background.
</objective>

<execution_context>
@/home/deploy-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/deploy-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/deploy-app/poc-lottery-online/.planning/PROJECT.md
@/home/deploy-app/poc-lottery-online/.planning/ROADMAP.md
@/home/deploy-app/poc-lottery-online/.planning/phases/01-v1-lottery-checker/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace @theme tokens with Thai lottery palette</name>
  <files>src/app/globals.css</files>

  <read_first>
    - src/app/globals.css — read the full file first; understand every existing token before changing
  </read_first>

  <action>
Rewrite the @theme block and body rule in src/app/globals.css.

KEEP unchanged:
- @import "tailwindcss"; line
- All --spacing-* tokens (xs through 3xl) — no change
- --font-sans token — no change
- @keyframes shake, fade-in-up — keep both
- * { box-sizing: border-box; } and html { -webkit-text-size-adjust: 100%; } — keep both

REMOVE entirely (per D-05, D-07 — Thai lottery palette replaces gold/cream):
- --color-gold: #D4A017
- --color-gold-light: #F5E6B3
- --color-gold-dark: #B8860B
- --color-cream: #FFFDF7
- --color-warm-gray: #F8F6F0
- --shadow-gold-glow: ... (the gold glow shadow)
- @keyframes glow-pulse (references gold rgb values — remove entirely)
- @keyframes prize-pop (no longer needed — remove)

ADD these new tokens (per D-07: green = brand/action, red = highlight/won):
```
  /* Thai lottery brand colors */
  --color-lottery-green: #006B3C;
  --color-lottery-green-dark: #004D2A;
  --color-lottery-green-light: #E6F4ED;
  --color-lottery-red: #C8102E;
  --color-lottery-red-light: #FDECEA;

  /* Semantic aliases — map onto the new palette */
  --color-win: #006B3C;          /* won state text + accents */
  --color-win-bg: #E6F4ED;       /* won state background fill */
  --color-error: #C8102E;        /* error state */
  --color-surface: #FFFFFF;
  --color-surface-2: #F5F5F5;
  --color-border: #E5E7EB;
  --color-text: #111827;
  --color-text-muted: #6B7280;
```

KEEP these shadows (neutral, not gold-specific):
- --shadow-card: 0 1px 3px rgb(0 0 0 / 0.06), 0 1px 2px rgb(0 0 0 / 0.04)
- --shadow-card-lg: 0 4px 12px rgb(0 0 0 / 0.08), 0 2px 4px rgb(0 0 0 / 0.04)

CHANGE the body rule (per D-06: flat layout, no ornamental gradient):
OLD:
  body {
    background: linear-gradient(180deg, #FFFDF7 0%, #FFF9EC 40%, #FFFFFF 100%);
    min-height: 100dvh;
  }
NEW:
  body {
    background: #FFFFFF;
    min-height: 100dvh;
  }

Final file structure order:
1. @import "tailwindcss";
2. @theme { ... } block with all kept + new tokens
3. * { box-sizing... }
4. html { -webkit-text-size-adjust... }
5. body { background: #FFFFFF; min-height: 100dvh; }
6. @keyframes shake { ... }
7. @keyframes fade-in-up { ... }
  </action>

  <verify>
    <automated>grep -n "lottery-green\|lottery-red\|#006B3C\|#C8102E" /home/deploy-app/poc-lottery-online/src/app/globals.css | head -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "color-lottery-green" globals.css returns at least 1 line
    - grep "color-lottery-red" globals.css returns at least 1 line
    - grep "color-gold:" globals.css returns 0 lines (token removed)
    - grep "color-cream" globals.css returns 0 lines (token removed)
    - grep "color-warm-gray" globals.css returns 0 lines (token removed)
    - grep "glow-pulse" globals.css returns 0 lines (keyframe removed)
    - grep "background: linear-gradient" globals.css returns 0 lines (gradient removed)
    - grep "background: #FFFFFF" globals.css returns 1 line (plain white body)
    - grep "spacing-xs\|spacing-sm\|spacing-md" globals.css returns lines (spacing kept)
    - grep "keyframes shake" globals.css returns 1 line (shake kept)
    - grep "keyframes fade-in-up" globals.css returns 1 line (fade-in-up kept)
  </acceptance_criteria>

  <done>globals.css @theme has Thai lottery green/red tokens. No gold/cream tokens remain. Body background is plain #FFFFFF. Spacing and font tokens unchanged. Shake + fade-in-up keyframes kept.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS build | Tailwind processes globals.css token names — invalid CSS variable names would break all styling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | globals.css @theme tokens | accept | CSS token names are build-time only; no runtime attack surface |
</threat_model>

<verification>
1. Run `npm run build` from project root — build must complete without CSS errors
2. Run dev server and visually confirm: body is white, no cream/gold gradient background
3. `grep "color-lottery-green\|color-lottery-red" src/app/globals.css` — must return results
4. `grep "color-gold:\|color-cream\|color-warm-gray" src/app/globals.css` — must return 0 lines
</verification>

<success_criteria>
- Thai lottery green (#006B3C) and red (#C8102E) tokens defined in @theme
- All gold/cream tokens removed
- Body background is plain white
- Build succeeds with no CSS errors
- Spacing tokens (xs–3xl) and font token untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-v1-lottery-checker/01-01-SUMMARY.md`
</output>
